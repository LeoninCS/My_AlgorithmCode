#include<bits/stdc++.h>
using namespace std;
using i64 = long long;

const int inf = 1e9;
const i64 INF = 1e18;

void solve()
{
	/*
	char tema,temb;
	i64 temc;
	cin >> tema >> temb >> temc;
	i64 cnt = 0;
	i64 ans = 0;
	while(1) {
		char a, b;
		i64 c;
		cin >> a >> b >> c;
		if(tema == temb && a == b && c - temc <= 1000) {
			cnt++;
			ans = max(cnt, ans);	
		}
		else cnt = 0;
		if(c == 0) break;
		tema = a;
		temb = b;
		temc = c;
	}
	cout << ans + 1;
	*/
	cout << 9;
}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0); cout.tie(0);
	
	int T = 1;
	//cin >> T;
	while(T--) solve();
	
	return 0;
}