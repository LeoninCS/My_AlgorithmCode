#include<bits/stdc++.h>
using namespace std;

void solve()
{
	cout << "All problems in computer science can be solved by another level of indirection.";
}

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
	int T = 1;
	//cin >> T;
	while(T--) solve();
	return 0;
} 
